name: sol_abi
description: >
  ABI JSON, metadata JSON, and NatSpec generation for the sol_pkgs Solidity compiler.
version: 0.1.0

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  sol_support: any
  sol_ast: any
  sol_types: any

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
