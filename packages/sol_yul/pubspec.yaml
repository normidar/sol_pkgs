name: sol_yul
description: >
  Yul intermediate representation, optimiser, and EVM dialect for the sol_pkgs compiler.
version: 0.1.0

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  sol_support: any
  sol_evm:
    path: ../sol_evm

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
