name: sol_types
description: >
  Solidity type system for the sol_pkgs compiler.
  Defines the runtime type lattice, implicit/explicit conversions, and mutability.
version: 0.1.0+2
repository: https://github.com/normidar/sol_pkgs/tree/main/packages/sol_types

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  sol_support: ^0.1.0+1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
