name: sol_types
description: >
  Solidity type system for the sol_pkgs compiler.
  Defines the runtime type lattice, implicit/explicit conversions, and mutability.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  sol_support:
    path: ../sol_support

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
