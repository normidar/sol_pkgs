name: sol_evm
description: >
  EVM opcode table, assembler, bytecode linker, and gas cost model
  for the sol_pkgs Solidity compiler.
version: 0.1.0+2
repository: https://github.com/normidar/sol_pkgs/tree/main/packages/sol_evm

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  sol_support: ^0.1.0+1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
